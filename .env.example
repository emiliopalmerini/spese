# This file documents environment variables used by the app and docker-compose.

# Server
PORT=8080
BASE_URL=http://localhost:8080

# Google Sheets
GOOGLE_SPREADSHEET_ID=
GOOGLE_SHEET_NAME=Spese
GOOGLE_CATEGORIES_SHEET_NAME=Categories
GOOGLE_SUBCATEGORIES_SHEET_NAME=Subcategories

# Choose one of the following auth methods:
# 1) Inline JSON credentials (Service Account)
# GOOGLE_CREDENTIALS_JSON='{"type":"service_account",...}'
# 2) Or a path to the credentials file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/creds.json

# Data backend (memory | sheets)
DATA_BACKEND=memory

# Smoke test (optional overrides for scripts/smoke.sh)
# CATEGORY=Casa
# SUBCATEGORY=Generale
